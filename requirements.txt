# Hugging Face model loading and tokenizer utilities
transformers>=4.41.0

# Deep learning backend used by Hugging Face models
torch>=2.2.0

# Text embedding model support for semantic search
sentence-transformers>=2.7.0

# Vector database used for storing and retrieving embeddings
chromadb>=0.5.0

# PDF parsing for large-document ingestion into the vector store
pypdf>=4.2.0

# Graph database driver for storing entities and relationships in Neo4j
neo4j>=5.20.0

# Loads configuration values from a local .env file
python-dotenv>=1.0.0

# Optional OpenAI API client if you later enable the cloud provider
openai>=1.0.0

# LangChain building blocks for chunking and retrieval strategies
langchain>=1.0.0
langchain-text-splitters>=1.0.0
langchain-chroma>=1.0.0
langchain-huggingface>=1.0.0

# Optional notebook support for live classroom demos
jupyter>=1.0.0
